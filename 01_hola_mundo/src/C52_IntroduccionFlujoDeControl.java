public class C52_IntroduccionFlujoDeControl {
    public static void main(String[] args) {

    }
}

/**
 * if (expresion){
 *     bloque a ejecutar
 * } else if(expresion){
 *     sentencias
 * }else {
 *     sentencias
 * }
 *
 * swich case para sentencias condicionales primitivo int, byte, short, char ,string ,enum
 *
 * switch(variable){
 *  case valor1:
 *      // ejecuta sentencia 1
 *      break;
 *  case valor2:
 *      // ejecuta sentencia 3
 *      break;
 *  case valor3:
 *      // ejecuta sentencia 3
 *      break;
 * default:
 *      // Si no existe coincidencia, se ejecuta la sentencia default
 * }
 *
 * Separadores
 * • Existen otros caracteres en Java con significado especial, son los
 * separadores:
 * Separador Descripción
 * ()
 * Se utilizan para contener condiciones en sentencias de flujo de control.
 * Contienen listas de parámetros, tanto en la definición de un método como en la llamada al mismo.
 * También se utilizan para modificar la precedencia en una expresión.
 * También para realizar conversiones de tipo
 *
 * {}
 * Se utilizan para definir bloques de código, definir ámbitos
 * También para contener los valores iniciales de los arreglos.
 *
 * []
 * Se utiliza tanto para declarar arreglos o matrices como para referenciar valores dentro de los mismos.
 *
 * ;
 * Separa sentencias.
 * También se utiliza para encadenar sentencias dentro de una estructura for.
 *
 * ,
 * Separa identificadores consecutivos en la declaración de variables y en las listas de parámetros de un método.
 *
 * .
 * Separa un nombre de atributo o método de una variable de referencia o objeto.
 * También separa nombre de paquete de los de un subpaquete o una clase.
 */